# Program to print prime numbers from 3 to 100

# Function to check if a number is prime
def is_prime(num):
    if num < 2:
        return False
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            return False
    return True

# Iterate over numbers from 3 to 100 and print if prime
for num in range(3, 101):
    if is_prime(num):
        print(num)

